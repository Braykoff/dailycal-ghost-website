services:
  ghost:
    image: ghost:6
    ports:
      - "2368:2368"
    environment:
      url: http://localhost:2368
      database__client: sqlite3
    volumes:
      - ./content:/var/lib/ghost/content
      - ./theme:/var/lib/ghost/content/themes/my-theme