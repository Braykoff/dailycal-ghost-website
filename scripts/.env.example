# Copy to scripts/.env and load before running scripts, e.g.:
#   set -a && source scripts/.env && set +a
#   python3 scripts/purge_content.py articles

# Local Ghost (docker compose)
GHOST_URL=http://localhost:2368
GHOST_ADMIN_EMAIL=admin@example.com
GHOST_ADMIN_PASSWORD=bogusAdminP@ssw0rd
GHOST_API_VERSION=v6.46

# Used by scripts/copy_blox_article.py
FEATURE_IMAGE_MAX_DIMENSION=1200
FEATURE_IMAGE_QUALITY=75
